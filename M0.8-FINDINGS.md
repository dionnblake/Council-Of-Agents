# Council of Agents: M0.8 Final Certification

**Date:** 2026-08-15  
**Scope:** M0.8 final certification only. No M1 implementation, production scaffolding, or architecture redesign.  
**Evidence labels:** `VERIFIED`, `OBSERVED`, `UNVERIFIED`, `BLOCKED`, `FAIL`, `PASS_WITH_DECLARED_LIMITATION`

## Final decision

# DO NOT START M1

Claude completed its final isolated certification after the provider session limit cleared. Antigravity's previously verified certification remains valid with its declared served-model limitation.

Codex authenticated successfully, but the live clean-room probe still loaded six real user skill files despite the explicit 1,080-entry supported disable configuration. Per the M0.8 instruction, Codex isolation is `FAILED` and all later Codex tests were stopped.

The planned three-seat Council therefore does not meet the M1 opening rule. This is the final feasibility certification. Do not continue with additional prompt or configuration tricks in a later feasibility pass. The remaining Codex decision is architectural: dedicated Windows identity, documented acceptance of user-context contamination, or a lineup change.

## Certification summary

| Seat | Certification | Decisive result |
|---|---|---|
| Codex | `FAIL` | 1,080 disable entries did not stop live loading from `%USERPROFILE%\.agents\skills` |
| Claude | `PASS` | 20/20 usable, 50/200/500 KB packet checks passed, stateless reconstruction passed |
| Antigravity | `PASS_WITH_DECLARED_LIMITATION` | 17/20 usable, one repair attempt, provider does not report served model |

## 1. Codex final certification

### Live isolation result

The Council Codex home was authenticated manually before this pass:

```text
CODEX_HOME=%LOCALAPPDATA%\council\codex-home
codex login status -> Logged in using ChatGPT
```

The Council configuration contained 1,080 explicit `[[skills.config]]` entries with `enabled = false`:

- 702 files from `%USERPROFILE%\.agents\skills`
- 378 non-system files from `%USERPROFILE%\.codex\skills`
- 6 `.system` files intentionally left enabled

The configuration parsed successfully with `codex exec --strict-config --help`.

The authenticated clean-room probe used:

```text
--json
--ephemeral
--ignore-rules
--skip-git-repo-check
--sandbox read-only
-C %LOCALAPPDATA%\council\m07-codex-work
-m gpt-5.6-luna
```

The model returned `CLEANROOM_OK`, but stderr still reported user skill loading failures for:

```text
%USERPROFILE%\.agents\skills\apify-scrape\SKILL.md
%USERPROFILE%\.agents\skills\beautiful-websites\SKILL.md
%USERPROFILE%\.agents\skills\jragmunch-cli\SKILL.md
%USERPROFILE%\.agents\skills\site-qualify\SKILL.md
%USERPROFILE%\.agents\skills\site-redesign\SKILL.md
%USERPROFILE%\.agents\skills\master-slide\plugins\frontend-slides\skills\frontend-slides\SKILL.md
```

This is a direct live failure of the supported disable mechanism in the tested Windows environment:

```text
CODEX_ISOLATION = FAILED
```

No further Codex conformance, packet, or stateless tests were run after this result, as required by the M0.8 instructions.

### Codex ambient result

The contaminated probe reported:

```text
input_tokens: 11,344
cached_input_tokens: 0
output_tokens: 8
```

This is not a clean isolated baseline because uncontrolled user skills were loaded. The valid answer for the clean ambient baseline is therefore:

```text
NO CLEAN BASELINE
CONTAMINATED OBSERVATION: 11,344 input tokens
```

### Codex certification fields

```text
CODEX
ISOLATION: FAIL - user skills loaded despite 1,080 disable entries
AUTH: PASS - Logged in using ChatGPT
AMBIENT: CONTAMINATED - 11,344 input tokens; not a valid clean baseline
SCHEMA: BLOCKED - stopped after isolation failure
PACKET: BLOCKED - stopped after isolation failure
STATELESS: BLOCKED - stopped after isolation failure
MODEL_IDENTITY: UNKNOWN - requested gpt-5.6-luna; served model not reported in the probe envelope
REPAIR_POLICY: NO REPAIR - isolation failure is terminal for this certification
CERTIFICATION: FAIL
```

## 2. Claude final certification

### Configuration and authentication

The existing isolated configuration was reused exactly as requested:

```text
%LOCALAPPDATA%\council\claude-cfg
```

No configuration file, credential file, settings file, or isolation architecture was edited by this pass. The test process used `--no-session-persistence`, disabled slash commands, and did not provide an API key or alternate base URL.

After the reported provider reset, the isolated minimal inference succeeded with valid structured output.

### Claude isolated ambient measurement

The first successful minimal call reported the following top-level usage:

```text
input_tokens: 20
cache_creation_input_tokens: 8,398
cache_read_input_tokens: 7,339
output_tokens: 1,438
```

The same response's `modelUsage` entry reported:

```text
inputTokens: 566
cacheReadInputTokens: 7,339
cacheCreationInputTokens: 8,398
outputTokens: 1,452
```

Both provider-reported forms are recorded because the CLI envelope exposes both top-level and model-specific counters. The current isolated baseline is therefore the complete tuple above, not a single input number.

### Claude schema conformance

The existing representative Council schema was run exactly 20 times in fresh non-persistent processes against the file-delivered M0.6 packet.

```text
usable: 20/20
schema-only: 0
failed: 0
conformance: 100%
```

All 20 calls returned the same requested and reported model:

```text
requested: claude-haiku-4-5-20251001
reported served model: claude-haiku-4-5-20251001
canonical model: claude-haiku-4-5
provider: firstParty
```

The post-hoc diagnostic detector flagged two calls because response text matched a broad `unexpected` pattern. Both calls were schema-valid and semantically usable. The detector did not classify either match as a hook, MCP, skill-load, or `CLAUDE.md` diagnostic. The diagnostic flags do not reduce the measured conformance result.

### Claude file-packet delivery

Each packet was read from the added read-only directory with the Claude `Read` tool. The model was required to return the exact terminal marker from the packet.

| Packet | Size | Marker | Result | Elapsed |
|---|---:|---|---|---:|
| `packet-50kb.md` | 51,200 bytes | `PACKET_END_50KB` | `PASS` | 20.46 s |
| `packet-200kb.md` | 204,800 bytes | `PACKET_END_200KB` | `PASS` | 151.46 s |
| `packet-500kb.md` | 512,000 bytes | `PACKET_END_500KB` | `PASS` | 189.66 s |

All three exact markers were returned in valid structured output.

The larger-packet timings are an operational limitation. They are not packet-delivery failures. Any later runtime would need a timeout budget that accommodates the observed 200 KB and 500 KB behavior.

### Claude stateless reconstruction

Turn 1 was a fresh Claude process with no session persistence. It read the original packet and returned:

```text
commitment: WOULD_STAKE
recommendation: M1 should begin with a small, two-seat core focused on recording structured positions and preserving evidence.
```

Turn 2 was a separate fresh Claude process with no resume. It read the original packet and the turn-2 packet. It:

- recovered the exact prior `WOULD_STAKE` commitment;
- recovered the prior two-seat recommendation;
- identified the peer claim about requiring a UI and active debate engine;
- addressed the peer claim;
- cited packet evidence;
- preserved the recommendation coherently.

### Claude certification fields

```text
CLAUDE
ISOLATION: PASS - isolated directory separation carried forward and no explicit uncontrolled-context errors observed
AUTH: PASS - isolated first-party Claude authentication and live inference succeeded
AMBIENT: PASS - input/cache/output tuple recorded above
SCHEMA: PASS - 20/20 usable, 100%
PACKET: PASS - 50 KB, 200 KB, and 500 KB exact markers returned
STATELESS: PASS - fresh-process two-turn reconstruction passed
MODEL_IDENTITY: VERIFIED_MATCH - requested and reported claude-haiku-4-5-20251001
REPAIR_POLICY: VALIDATION AND QUARANTINE ONLY - conformance is >=95%
CERTIFICATION: PASS
```

## 3. Antigravity final certification

The M0.6 Antigravity evidence remains valid and is carried forward without additional calls in M0.8.

```text
ANTIGRAVITY
ISOLATION: PASS
AUTH: PASS
AMBIENT: PASS - 22,644 input, 0 cache-read, 77 output, 22,721 total
SCHEMA: 17/20 = 85%
PACKET: PASS
STATELESS: PASS
MODEL_IDENTITY: PROVIDER_DOES_NOT_REPORT
REPAIR_POLICY: ONE REPAIR ATTEMPT
CERTIFICATION: PASS_WITH_DECLARED_LIMITATION
```

The Antigravity limitation is accepted because the requested model was explicitly pinned, the provider is known, and the provider does not expose a served-model field. No served model is fabricated.

## 4. Required answers

### 1. Did the 1,080 Codex skill-disable entries work during live authenticated inference?

**NO.** The authenticated probe still loaded six real `.agents\skills` files. Codex isolation is `FAIL`.

### 2. What is Codex's clean ambient baseline?

**None.** The only live measurement was contaminated. It reported 11,344 input tokens and cannot be used as a clean baseline.

### 3. What is Codex's measured schema conformance?

**Not measured.** The required suite was stopped immediately after the isolation failure.

### 4. Does Codex file-packet delivery pass?

**Not measured.** Testing stopped after isolation failure.

### 5. Does Codex stateless handoff pass?

**Not measured.** Testing stopped after isolation failure.

### 6. Did Claude provider availability return?

**YES.** The post-reset minimal inference succeeded, followed by the full final test suite.

### 7. What is Claude's isolated ambient baseline?

The successful minimal-call envelope reported:

```text
top-level input: 20
cache creation: 8,398
cache read: 7,339
output: 1,438
modelUsage input: 566
modelUsage output: 1,452
```

### 8. What is Claude's measured schema conformance?

**20/20 usable = 100%.**

### 9. Does Claude file-packet delivery pass?

**YES.** The 50 KB, 200 KB, and 500 KB packet markers were all returned successfully.

### 10. Does Claude stateless handoff pass?

**YES.** Two fresh non-resumed processes reconstructed the prior position, addressed the peer claim, preserved the recommendation, and cited evidence.

### 11. What repair policy applies to all three seats?

| Seat | Conformance | Repair policy |
|---|---:|---|
| Codex | Isolation failed before conformance | No repair. Stop at isolation failure; do not repair contaminated output. |
| Claude | 20/20 = 100% | Validation and quarantine only. No automatic repair. |
| Antigravity | 17/20 = 85% | One repair attempt, with raw-response retention and quarantine. |

### 12. What served-model limitations remain?

| Seat | Requested model | Served-model evidence | Status |
|---|---|---|---|
| Codex | `gpt-5.6-luna` | Not reported in the probe envelope | `UNKNOWN` |
| Claude | `claude-haiku-4-5-20251001` | `modelUsage` reported the same full model | `VERIFIED_MATCH` |
| Antigravity | `gemini-3.7-flash-low` | Provider does not report served model | `PROVIDER_DOES_NOT_REPORT` |

### 13. Are there any P0 blockers?

**YES.** Codex isolation remains a P0 blocker for the planned three-seat Council. The supported 1,080-entry suppression mechanism did not stop uncontrolled user skill loading.

Claude has no remaining certification blocker. Its large-packet latency and Antigravity's served-model opacity are declared operational limitations, not reasons to misclassify either seat.

### 14. Should M1 open?

No. The planned product is a three-seat Council, and Codex is `FAIL`. The minimum three-seat opening condition is not met.

## 5. Final three-seat certification

```text
CODEX
ISOLATION: FAIL
AUTH: PASS
AMBIENT: CONTAMINATED, no clean baseline
SCHEMA: BLOCKED
PACKET: BLOCKED
STATELESS: BLOCKED
MODEL_IDENTITY: UNKNOWN
REPAIR_POLICY: NO REPAIR; isolation failure is terminal
CERTIFICATION: FAIL

CLAUDE
ISOLATION: PASS
AUTH: PASS
AMBIENT: PASS
SCHEMA: 20/20 = 100%
PACKET: PASS
STATELESS: PASS
MODEL_IDENTITY: VERIFIED_MATCH
REPAIR_POLICY: VALIDATION AND QUARANTINE ONLY
CERTIFICATION: PASS

ANTIGRAVITY
ISOLATION: PASS
AUTH: PASS
AMBIENT: PASS
SCHEMA: 17/20 = 85%
PACKET: PASS
STATELESS: PASS
MODEL_IDENTITY: PROVIDER_DOES_NOT_REPORT
REPAIR_POLICY: ONE REPAIR ATTEMPT
CERTIFICATION: PASS_WITH_DECLARED_LIMITATION
```

## 6. Final M1 opening rule result

The actual three-seat product is not certified for M1 because Codex failed isolation during live authenticated inference.

Do not lower the standard because feasibility testing has already consumed time. Stop M0.8 here.

The remaining decision is architectural, not another prompt/configuration experiment:

- use a dedicated Windows identity for Codex;
- explicitly accept and document Codex user-context contamination; or
- change the planned Council lineup.

No choice among those options is made in this findings document.

## Side effects and safety

- Reused `%LOCALAPPDATA%\council\claude-cfg` without editing its configuration or credentials.
- Did not copy, rotate, or expose credentials.
- Did not modify the real `.agents\skills` or `.codex\skills` trees.
- Ran Codex in the existing isolated Council home and safe work directory.
- Used read-only file packet access for Claude.
- Did not create production code, a debate engine, a UI, a master-prompt compiler, or M1 scaffolding.
- No credential values, API keys, account identifiers, or provider secrets were written to this workspace or this findings file.

## Verification record

| Check | Result |
|---|---|
| Codex Council authentication | `PASS`, logged in using ChatGPT |
| Codex supported skill suppression | `FAIL`, six `.agents\skills` files still loaded live |
| Codex later gates | Correctly stopped after isolation failure |
| Claude provider availability | `PASS` after reset |
| Claude 20-call conformance | `PASS`, 20/20 usable |
| Claude packet delivery | `PASS`, 50/200/500 KB markers returned |
| Claude stateless handoff | `PASS`, fresh-process pair passed |
| Antigravity evidence | Carried forward, `PASS_WITH_DECLARED_LIMITATION` |
| Generic project verifier | Expected `UNVERIFIED`: findings-only workspace has no runnable project manifest |

## Sources

- [OpenAI Codex skills documentation](https://developers.openai.com/codex/skills)
- [OpenAI Codex configuration reference](https://developers.openai.com/codex/config-reference/)
- [Google Antigravity CLI installation and authentication](https://antigravity.google/docs/cli/install)
- `M0-FINDINGS.md`, `M0-RAW-RESULTS.md`, `M0.5-FINDINGS.md`, `M0.6-FINDINGS.md`, and `M0.7-FINDINGS.md` in this workspace
