# Council of Agents: M0.7 Findings

**Date:** 2026-08-15  
**Scope:** M0.7 final closure only. No M1 implementation, production scaffolding, or architecture redesign.  
**Evidence labels:** `VERIFIED`, `OBSERVED`, `UNVERIFIED`, `BLOCKED`, `PASS_WITH_DECLARED_LIMITATION`

## Final decision

# DO NOT START M1

M0.7 did not close the remaining Council gates.

The supported Codex skill-disable configuration was generated and accepted by the Codex configuration parser, but the intended Council Codex home is not authenticated. The M0.7 instructions explicitly prohibit copying normal credentials or authenticating on the owner's behalf. Therefore no authenticated Codex clean-room inference was run, and Codex contamination elimination remains `UNVERIFIED` rather than `PASS` or `FAIL`.

Claude's isolated authentication still exists, but the one permitted minimal retest again returned a provider `429 session limit` before inference. Reported inference usage was zero. No repeated calls were spent.

Antigravity remains eligible with its previously verified limitation: 17/20 schema conformance and no provider-reported served-model field.

The result is fail-closed. It does not claim that Codex or Claude can never pass. It means the required evidence was not available during this closure pass.

## M0.7 changes from M0.6

### Codex supported suppression configuration

The real local skill roots were inventoried without modifying them:

| Root | Files found | Treatment |
|---|---:|---|
| `%USERPROFILE%\.agents\skills` | 702 | Explicitly disabled in Council config |
| `%USERPROFILE%\.codex\skills` outside `.system` | 378 | Explicitly disabled in Council config |
| `%USERPROFILE%\.codex\skills\.system` | 6 | Left enabled as provider/system skills |

The generated Council configuration is:

```text
%LOCALAPPDATA%\council\codex-home\config.toml
```

It contains 1,080 `[[skills.config]]` entries with `enabled = false`, one for every observed non-system `SKILL.md` in the two local roots. The six `.system` entries were not disabled.

The configuration was accepted by:

```text
codex exec --strict-config --help
```

The command exited `0`. This proves that the generated configuration is syntactically accepted by the installed Codex CLI. It does not prove that a live authenticated session will stop loading all user context.

### Codex authentication gate

Using only the intended Council path:

```text
CODEX_HOME=%LOCALAPPDATA%\council\codex-home
```

the status check returned:

```text
Not logged in
```

Exit code was `1`.

No normal Codex credentials were copied. No API key was supplied. No Codex inference, 20-call conformance suite, packet test, or stateless test was run after this blocker.

### Claude retest

The existing isolated directory was reused without rebuilding its architecture:

```text
%LOCALAPPDATA%\council\claude-cfg
```

The one minimal call returned:

```text
api_error_status: 429
result: You've hit your session limit
reset: 7:30am (<LOCAL_TIMEZONE>)
```

Inference usage was zero. This is a provider-availability block, not a schema failure.

## Required answers

### 1. Did supported Codex skill disabling eliminate `$HOME/.agents/skills` contamination?

**UNVERIFIED.**

The supported per-skill configuration was generated for all 702 discovered `.agents\skills\SKILL.md` files and parsed successfully. However, the required live clean-room inference could not run because the Council Codex home was not authenticated.

There is no valid M0.7 evidence that an authenticated Codex process no longer loads the real user skill tree. There is also no valid evidence that it still loads it after the generated disable entries. The correct status is `UNVERIFIED`.

The non-system files in the observed `.codex\skills` mirror were also disabled. The six `.system` files were left enabled because the prompt permits unavoidable provider/system context and forbids disabling it without a documented reason.

### 2. What is Codex's clean isolated ambient baseline?

**UNVERIFIED. No numeric baseline exists for M0.7.**

The prior normal-home value of roughly 30.6K input tokens remains invalid as a Council-isolated baseline. The Council path never reached inference.

### 3. What is Codex schema conformance?

**0/20 measured: BLOCKED.**

The suite was not run because authentication was missing. No normal-home result was substituted.

### 4. Does Codex stateless reconstruction pass?

**BLOCKED and therefore UNVERIFIED.**

No authenticated first or second turn was run.

### 5. Does Codex file-packet delivery pass?

**BLOCKED and therefore UNVERIFIED.**

The 50 KB, 200 KB, and 500 KB packet checks were not run for Codex.

### 6. Is Claude currently available?

**NO: `CLAUDE = BLOCKED_PROVIDER_AVAILABILITY`.**

The isolated authentication status remained present, but the first minimal inference attempt was rejected with a provider `429 session limit` before inference. The result contained zero input, cache-read, and output usage.

No further Claude calls were attempted after the one permitted retest.

### 7. What is Claude's isolated ambient baseline?

**UNVERIFIED.**

The current call returned zero usage because the provider rejected it before inference. Zero is not an ambient baseline.

The earlier normal-home Claude measurements, approximately 34.8K to 40.3K input tokens, must not be used as the isolated Council value.

### 8. What is Claude schema conformance?

**0/20 measured: BLOCKED.**

The provider did not reach inference, so no response was counted as a schema failure.

### 9. Does Claude stateless reconstruction pass?

**BLOCKED and therefore UNVERIFIED.**

The two-turn reconstruction was not attempted after the provider limit appeared.

### 10. What repair policy applies to each seat?

The previously committed thresholds remain unchanged:

```text
>=95%   validation and quarantine only
80-94%  one repair attempt
<80%    simplify or revise the schema before adding repair machinery
```

| Seat | Conformance | Repair policy |
|---|---:|---|
| Codex | `0/20`, blocked before inference | No automatic repair. Parse, schema-validate, semantic-validate, classify typed failure, quarantine, and retain raw output until measured. |
| Claude | `0/20`, provider-blocked before inference | No automatic repair. Parse, schema-validate, semantic-validate, classify typed failure, quarantine, and retain raw output until measured. |
| Antigravity | `17/20 = 85%` | One repair attempt is allowed under the precommitted 80-94% rule. Preserve the raw response and quarantine repair results. |

Antigravity's repair allowance must not be generalized to Codex or Claude before valid conformance measurements exist.

### 11. What model-identity limitations remain?

The Council must record these fields separately:

```text
REQUESTED_MODEL
REPORTED_SERVED_MODEL
SERVING_IDENTITY_STATUS
```

| Seat | Requested model | Reported served model | Status |
|---|---|---|---|
| Codex | Not measured | Not measured | `UNKNOWN` |
| Claude | `claude-haiku-4-5-20251001` | None because inference did not start | `UNKNOWN` |
| Antigravity | `gemini-3.7-flash-low` | Provider did not report one | `PROVIDER_DOES_NOT_REPORT` |

For Antigravity, `PROVIDER_DOES_NOT_REPORT` is an accepted declared limitation because the requested model was explicitly pinned and the provider is known. No actual served model is fabricated.

For Codex and Claude, the status remains `UNKNOWN` because the isolated calls did not produce valid serving evidence.

### 12. Are any P0 blockers left?

**YES.**

Remaining P0 blockers are:

1. The Council Codex home is unauthenticated.
2. Codex user-skill suppression has not been tested through a live authenticated inference.
3. Codex ambient usage, conformance, packet delivery, model identity, and statelessness are unmeasured.
4. Claude is provider-blocked by the session limit.
5. Claude ambient usage, conformance, packet delivery, model identity, and statelessness are unmeasured.
6. The three-seat Council does not yet have complete served-model evidence.

### 13. Should M1 open?

M1 remains closed.

The M1 opening rule requires Codex and Claude to be `PASS` or `PASS_WITH_DECLARED_LIMITATION`, plus Antigravity at one of those statuses, for the planned three-seat Council.

Antigravity satisfies its individual gate with a declared limitation. Codex and Claude do not. The minimum three-seat condition is therefore not met.

## Seat certification

```text
CODEX
ISOLATION: UNVERIFIED
AUTH: BLOCKED - Council home reports Not logged in
SCHEMA: BLOCKED - 0/20 measured
PACKET: BLOCKED - not run
STATELESS: BLOCKED - not run
MODEL_IDENTITY: UNKNOWN
CERTIFICATION: BLOCKED

CLAUDE
ISOLATION: PASS for directory separation, carried forward from M0.5
AUTH: PASS for isolated auth status, but live provider availability is blocked
SCHEMA: BLOCKED - 0/20 measured because provider returned 429 before inference
PACKET: BLOCKED - not run
STATELESS: BLOCKED - not run
MODEL_IDENTITY: UNKNOWN
CERTIFICATION: BLOCKED

ANTIGRAVITY
ISOLATION: PASS for the file-driven/stateless seat tested in M0.5/M0.6
AUTH: PASS, carried forward
SCHEMA: 17/20 = 85%, one repair attempt allowed
PACKET: PASS for the 1,078-byte, 50 KB, 200 KB, and 500 KB checks
STATELESS: PASS for one controlled fresh-process pair
MODEL_IDENTITY: PASS_WITH_DECLARED_LIMITATION - provider does not report served model
CERTIFICATION: PASS_WITH_DECLARED_LIMITATION
```

## Exact Codex action required from the owner

The following must be run interactively by the owner, not by this agent:

```powershell
$env:CODEX_HOME = "$env:LOCALAPPDATA\council\codex-home"
Remove-Item Env:OPENAI_API_KEY -ErrorAction SilentlyContinue
Remove-Item Env:OPENAI_BASE_URL -ErrorAction SilentlyContinue
Remove-Item Env:OPENAI_API_BASE -ErrorAction SilentlyContinue
codex login --device-auth
codex login status
```

This must use the Council home shown above and the ChatGPT/subscription authentication flow. Do not use `codex login --with-api-key`. Do not copy `auth.json` or OAuth tokens from the normal Codex home.

After `codex login status` reports authenticated, the next pass can run the Codex clean-room probe and only then proceed to conformance, packet, and stateless tests.

## Required next pass

### Codex

1. the owner completes the interactive Council-home login.
2. Verify no `OPENAI_API_KEY`, alternate provider, custom base URL, user hooks, plugins, or MCP are present in the Council process.
3. Run one minimal inference in the empty safe working directory.
4. Capture requested model, reported model, input tokens, unexpected skill loading, hooks, AGENTS instructions, MCP/plugins, and session status.
5. If any uncontrolled user skill still loads after explicit disable entries, mark Codex isolation `FAIL` and stop using more environment-variable workarounds.
6. Only after isolation passes, run the 20-call conformance suite, packet sizes, and two-turn stateless test.

### Claude

1. Wait for provider availability after the reported session reset.
2. Reuse `%LOCALAPPDATA%\council\claude-cfg`.
3. Run one minimal inference first.
4. If it still returns `429 session limit`, retain `CLAUDE = BLOCKED_PROVIDER_AVAILABILITY` and stop.
5. If inference succeeds, immediately capture isolated ambient usage, requested model, served-model reporting, and unexpected configuration/memory/hooks/MCP.
6. Only then run the 20-call conformance, file-packet, and stateless suites.

### Council-wide

Do not use normal-home measurements as isolated baselines. Keep provider availability failures separate from schema failures. Keep requested model and served-model evidence separate. Preserve raw outputs and failure classifications.

## Side effects and safety

- Created `%LOCALAPPDATA%\council\codex-home\config.toml` with 1,080 explicit non-system skill-disable entries.
- Created the empty safe working directory `%LOCALAPPDATA%\council\m07-codex-work`.
- Created the synthetic Claude test directory `%LOCALAPPDATA%\council\m07-claude-work`.
- Did not rename, move, delete, ACL-block, or modify the real `.agents\skills` tree.
- Did not copy normal Codex credentials or OAuth tokens.
- Did not authenticate on the owner's behalf.
- Did not create production code, a debate engine, a UI, a master-prompt compiler, or M1 scaffolding.
- No credential values, API keys, account identifiers, or provider secrets were written to this workspace or this findings file.

## Verification record

| Check | Result |
|---|---|
| Codex generated config syntax | `VERIFIED` by `codex exec --strict-config --help`, exit `0` |
| Codex Council authentication | `BLOCKED`, `codex login status` returned `Not logged in` |
| Codex live clean-room inference | Not run by instruction |
| Claude minimal isolated retest | `BLOCKED`, provider `429 session limit`, zero usage |
| Antigravity evidence | Carried forward from M0.5/M0.6, including 17/20 conformance and Job Object cleanup |
| Generic project verifier | `UNVERIFIED`: findings-only workspace has no recognized runnable project manifest |

## Sources

- [OpenAI Codex skills documentation](https://developers.openai.com/codex/skills)
- [OpenAI Codex configuration reference](https://developers.openai.com/codex/config-reference/)
- [Google Antigravity CLI installation and authentication](https://antigravity.google/docs/cli/install)
- `M0-FINDINGS.md`, `M0-RAW-RESULTS.md`, and `M0.5-FINDINGS.md` in this workspace
